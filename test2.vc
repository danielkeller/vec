int:[String] main {args}
(
    int foo = 7;
    int bar;
    bool undef;

    undef = foo == bar + 1 || foo < bar;

    if (undef)
    (
        //bar = (
        if (foo == 7)
            foo = 8;
        else
            int a = foo + 1;
        //);
    )
    else
    (
        if (bar == 1)
            foo = 9;
        else
            int b = foo + 6;
    )
    bar;
);
